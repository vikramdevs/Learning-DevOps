man = Manual, it means through man command i can get the details of other command eg. man ls so i get the details fo ls command


ls = List
ls -ltr = Lists files and directories in long format, sorted by modification time, in reverse order (oldest first). Oldest files appear at the top, newest at the bottom.

pwd = Present Working Directory(Where I am)


cd = Change Directory (To change Directory)
cd .. = To move 1 step Back
cd ../.. = Move 2 Step Back

touch File_name = to create file



cat file_name = used to display the contents of a file on the terminal.see the code
./file_name = run the code

chmod = change the permission mode of a file or directory.



nano , vi and vim editor  In Vim esc then i to INSERT and :wq to save and exit  and for only exit :q

r -> 4
w -> 2
x -> 1



mkdir folder_name = to create directory or folder


 
df = disk free Shows disk space usage (storage). df -h

free = Shows RAM and swap memory usage. free -h

nproc = Shows the number of CPU cores available to the system.

top = Shows real-time system performance.

| Command | Checks             | What it shows                             |
| ------- | ------------------ | ----------------------------------------- |
| `df`    | Disk               | HDD / SSD usage (total, used, available)  |
| `free`  | Memory             | RAM and Swap usage                        |
| `nproc` | CPU                | Number of available CPU cores             |
| `top`   | System performance | Live CPU, memory usage, running processes |



set -x = debug mode for shell scripts.



ps -ef = show every running process with full details.

It’s a snapshot of what’s running right now on the system.

ps → process status

-e → all processes (system + users)

-f → full format (extra columns)

to search for a process use and grep is a search command
ps -ef | grep "process_name" = to find a specific process in the list of running processes. eg. ps-ef | grep "nginx"


awk = A powerful text-processing tool used for pattern scanning and processing. It is commonly used for extracting and manipulating data from text files or command output.
example: ps -ef | grep "nginx" | awk '{print $2}' = this command will give only the process id of nginx process

set -e = Script turant exit ho jata hai agar koi command fail (non-zero exit code) ho.

set -o pipefail = Pipeline (|) ka exit status fail hoga agar pipeline ka koi bhi command fail ho.



curl = Command-line tool for transferring data with URLs. Commonly used to make HTTP requests to web servers.
example: curl -I www.example.com = This command fetches only the HTTP headers of the specified URL.

Postman = Postman ek GUI tool hai jo APIs ko manually test karne ke liye use hota hai.

Postman → manual testing, learning, debugging (click-based)
curl → automation, scripts, CI/CD (command-line)
wget → ek command-line tool hai jo internet se files download karne ke liye use hota hai. eg:- wget https://example.com/file.zip

